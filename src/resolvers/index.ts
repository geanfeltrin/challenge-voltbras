import Query from './Query';
import Mutation from './Mutation';

// não curti muito o fato de as regras de negócio estarem nos resolvers e não no próprio restdatasource
export default {
  Query,
  Mutation,
};
